@tool
extends EditorScript

## Скрипт для налаштування OpenRouter API ключа
## Запустіть через Editor → Run Script
##
## ІНСТРУКЦІЯ:
## 1. Замініть "YOUR_API_KEY_HERE" на свій API ключ від OpenRouter
## 2. Відкрийте Editor → Run Script
## 3. Виберіть цей файл та натисніть Run

func _run() -> void:
	# ⬇️⬇️⬇️ ВСТАВТЕ СВІЙ API КЛЮЧ СЮДИ ⬇️⬇️⬇️
	var api_key = "YOUR_API_KEY_HERE"  # <-- ЗАМІНІТЬ НА СВІЙ КЛЮЧ!
	# ⬆️⬆️⬆️ ВСТАВТЕ СВІЙ API КЛЮЧ СЮДИ ⬆️⬆️⬆️
	
	if api_key == "YOUR_API_KEY_HERE" or api_key.is_empty():
		print("❌ ПОМИЛКА: Вставте свій API ключ у змінну api_key!")
		print("📝 Відкрийте файл setup_openrouter_api.gd та замініть YOUR_API_KEY_HERE")
		return
	
	# Перевіряємо, чи клас доступний
	if not ClassDB.class_exists("LLMConfigManager"):
		print("❌ ПОМИЛКА: LLMConfigManager не знайдено. Переконайтеся, що AI Assistant Hub плагін активовано.")
		return
	
	# Зберігаємо API ключ для OpenRouter
	var config = LLMConfigManager.new("openrouter_api")
	config.save_key(api_key)
	
	print("✅ API ключ успішно збережено для OpenRouter!")
	print("")
	print("📝 Наступні кроки:")
	print("   1. Відкрийте вкладку 'AI Hub' (внизу редактора)")
	print("   2. Виберіть провайдер 'OpenRouter' зі списку")
	print("   3. Натисніть 'Refresh Models' щоб побачити доступні GPT моделі")
	print("   4. Виберіть модель (рекомендую: openai/gpt-4-turbo)")
	print("   5. Натисніть 'New assistant type' щоб створити асистента")
	print("")
	print("💡 Найкращі моделі для кодування:")
	print("   - openai/gpt-4-turbo (найкраща)")
	print("   - openai/gpt-4")
	print("   - openai/gpt-3.5-turbo (швидка та дешевша)")
