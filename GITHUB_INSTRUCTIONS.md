# Інструкція: Як викласти проєкт на GitHub

## Крок 1: Налаштування Git (якщо ще не зроблено)

Відкрийте PowerShell або Git Bash і виконайте:

```bash
git config --global user.name "Твоє Ім'я"
git config --global user.email "твій@email.com"
```

Або тільки для цього репозиторію:

```bash
cd "E:\GAME DEV STUD\TEST DIALOGUE"
git config user.name "Твоє Ім'я"
git config user.email "твій@email.com"
```

## Крок 2: Зробити commit

```bash
cd "E:\GAME DEV STUD\TEST DIALOGUE"
git commit -m "Initial commit: Dialogue Manager demo '4 друзі в барі'"
```

## Крок 3: Створити репозиторій на GitHub

1. Зайди на [github.com](https://github.com)
2. Натисни кнопку **"+"** (праворуч вгорі) → **"New repository"**
3. Заповни:
   - **Repository name**: `godot-dialogue-manager-demo` (або інша назва)
   - **Description**: "Dialogue Manager 3 demo - 4 друзі в барі"
   - **Public** або **Private** (на твій вибір)
   - **НЕ** стави галочку "Initialize with README" (бо він вже є)
4. Натисни **"Create repository"**

## Крок 4: Підключити локальний репозиторій до GitHub

Після створення репозиторію GitHub покаже інструкції. Виконай:

```bash
cd "E:\GAME DEV STUD\TEST DIALOGUE"
git remote add origin https://github.com/ТВІЙ_ЮЗЕРНЕЙМ/НАЗВА_РЕПОЗИТОРІЮ.git
git branch -M main
git push -u origin main
```

**Заміни:**
- `ТВІЙ_ЮЗЕРНЕЙМ` - твій GitHub username
- `НАЗВА_РЕПОЗИТОРІЮ` - назва репозиторію, яку ти створив

## Крок 5: Якщо використовуєш SSH (за бажанням)

Замість HTTPS можна використати SSH:

```bash
git remote add origin git@github.com:ТВІЙ_ЮЗЕРНЕЙМ/НАЗВА_РЕПОЗИТОРІЮ.git
```

## Готово! 🎉

Тепер твій проєкт на GitHub. Можна:
- Ділитися посиланням
- Клонувати на інші комп'ютери
- Приймати pull requests
- Створювати issues

## Наступні зміни

Коли зробиш зміни в проєкті:

```bash
git add .
git commit -m "Опис змін"
git push
```

## Важливо

- Файли `.godot/` та `*.import` автоматично ігноруються через `.gitignore`
- Архів `godot_dialogue_manager-*.zip` також включений (якщо хочеш видалити - додай в `.gitignore`)
