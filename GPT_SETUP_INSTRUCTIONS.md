# Налаштування GPT через OpenRouter

## Які моделі GPT найкращі для кодування?

### 🥇 **Рекомендовані моделі (від найкращої):**

1. **`openai/gpt-4-turbo`** ⭐ НАЙКРАЩА
   - Найрозумніша модель для складних завдань
   - Чудово розуміє контекст коду
   - Підтримує багато мов програмування
   - Дорожча, але найкраща якість

2. **`openai/gpt-4`**
   - Дуже хороша якість відповідей
   - Хороша для складних завдань
   - Трохи повільніша за turbo версію

3. **`openai/gpt-3.5-turbo`**
   - Швидка та дешевша
   - Добре для простих завдань
   - Менша якість за GPT-4

### 📋 Інші корисні моделі:
- `openai/gpt-4o` - нова оптимізована версія
- `openai/gpt-4o-mini` - легка версія GPT-4o

## Як вставити API ключ

### Варіант 1: Через UI (найпростіше) ✅

1. Відкрийте проект у Godot
2. Відкрийте вкладку **"AI Hub"** (внизу редактора)
3. У випадаючому списку **"LLM Provider"** виберіть **"OpenRouter"**
4. У полі **"API key"** вставте свій API ключ
5. Натисніть **"Refresh Models"** - з'явиться список доступних моделей
6. Виберіть модель (наприклад, `openai/gpt-4-turbo`)
7. Натисніть **"New assistant type"** щоб створити асистента

### Варіант 2: Через скрипт (якщо хочете автоматично)

1. Відкрийте файл `setup_openrouter_api.gd`
2. Замініть `YOUR_API_KEY_HERE` на свій API ключ
3. Відкрийте **Editor → Run Script**
4. Виберіть `setup_openrouter_api.gd` та натисніть **Run**

## Де отримати API ключ?

### Якщо у вас є OpenAI API ключ:
1. Перейдіть на [OpenRouter.ai](https://openrouter.ai/)
2. Зареєструйтеся або увійдіть
3. Перейдіть в [Keys](https://openrouter.ai/keys)
4. Створіть новий API ключ
5. Скопіюйте ключ

### Якщо у вас вже є OpenRouter ключ:
Просто вставте його в поле "API key" в AI Hub.

## Як використовувати після налаштування

1. Відкрийте **AI Hub**
2. Натисніть на кнопку вашого асистента (або створіть новий)
3. Починайте спілкування:
   - "Допоможи написати функцію для діалогової системи"
   - "Як створити сцену з персонажами?"
   - "Поясни, як працює Dialogue Manager"
   - "Напиши код для обробки кліків миші"

## Примітки

- API ключ зберігається локально в `user://ai_assistant_hub/llm_settings.cfg`
- Ключ не потрапляє в Git (файл в `user://` не синхронізується)
- Модель `gpt-4-turbo` найкраща для кодування, але дорожча
- Модель `gpt-3.5-turbo` швидша та дешевша для простих завдань
