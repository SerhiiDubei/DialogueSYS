# 🚀 Інструкція по деплою на GitHub Pages

## ✅ Автоматичний деплой (рекомендовано)

### 1. Підготовка репозиторію

1. Переконайся що файл `.github/workflows/deploy-web.yml` в репозиторії
2. Переконайся що файл `export_presets.cfg` в корені проекту

### 2. Налаштування GitHub Pages

1. Йди на **GitHub** → твій репозиторій
2. **Settings** → **Pages** (ліва панель)
3. В **Source** обери:
   - **Branch:** `gh-pages`
   - **Folder:** `/ (root)`
4. Натисни **Save**

### 3. Налаштування GitHub Actions

1. **Settings** → **Actions** → **General**
2. В **Workflow permissions** обери:
   - ✅ **Read and write permissions**
3. Натисни **Save**

### 4. Запуск

1. Зроби будь-який коміт в `main` гілку
2. GitHub Actions автоматично:
   - Зібере гру для Web
   - Задеплоїть на `gh-pages` гілку
3. Гра буде доступна на:
   ```
   https://твій-username.github.io/DialogueSYS/
   ```

### 5. Перевірка статусу білду

1. **Actions** таб в репозиторії
2. Дивись на статус **Deploy to GitHub Pages**
3. Якщо зелена галочка ✅ — готово!
4. Якщо червоний хрестик ❌ — перевір логи

---

## 🔧 Ручний деплой (якщо щось не працює)

### 1. Експорт в Godot

1. Відкрий проект в **Godot Editor**
2. **Project** → **Export...**
3. Якщо немає **Web** пресету:
   - Натисни **Add...** → **Web**
4. Обери **Web** preset
5. Натисни **Export Project**
6. Обери папку `build/web/`
7. Назви файл `index.html`
8. Натисни **Save**

### 2. Деплой на GitHub Pages

**Варіант A: Через Terminal**

```bash
# 1. Створи гілку gh-pages
git checkout --orphan gh-pages

# 2. Видали всі файли
git rm -rf .

# 3. Скопіюй файли з build/web/
cp -r build/web/* .

# 4. Додай .nojekyll щоб GitHub не ігнорував файли з _
touch .nojekyll

# 5. Закомить
git add .
git commit -m "Deploy to GitHub Pages"

# 6. Запуш
git push origin gh-pages

# 7. Повернись на main
git checkout main
```

**Варіант B: Через GitHub Web UI**

1. Збудуй проект локально (пункт 1)
2. Створи новий репозиторій або папку `gh-pages` branch
3. Завантаж всі файли з `build/web/` туди
4. Налаштуй GitHub Pages (Source → `gh-pages`)

---

## ⚙️ Налаштування .gitignore

Додай в `.gitignore`:

```
# Godot build artifacts
build/
.godot/
*.import
export_presets.cfg.bak
```

**НО!** `export_presets.cfg` **МАЄ** бути в репозиторії для GitHub Actions!

---

## 🐛 Troubleshooting

### Проблема: "Failed to export"

**Рішення:**
1. Перевір що встановлені **Export Templates** в Godot
2. **Editor** → **Manage Export Templates**
3. Завантаж шаблони для версії 4.5.1

### Проблема: "404 Not Found" на GitHub Pages

**Рішення:**
1. Перевір що гілка `gh-pages` існує
2. Перевір що в **Settings → Pages** обрано правильну гілку
3. Почекай 1-2 хвилини після пушу

### Проблема: Гра не завантажується (чорний екран)

**Рішення:**
1. Відкрий **Developer Tools** (F12) в браузері
2. Дивись на помилки в **Console**
3. Можливо треба налаштувати **SharedArrayBuffer** headers:
   - Створи файл `.github/workflows/pages.yml` з правильними headers

### Проблема: "Cross-Origin" помилки

**Рішення:**
Створи файл `_headers` в `build/web/`:

```
/*
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Opener-Policy: same-origin
```

---

## 📊 Моніторинг

Після деплою перевір:
- ✅ Гра завантажується
- ✅ Діалоги працюють
- ✅ Збереження працює (LocalStorage)
- ✅ UI відображається правильно

---

## 🔗 Корисні посилання

- [Godot Web Export Docs](https://docs.godotengine.org/en/stable/tutorials/export/exporting_for_web.html)
- [GitHub Pages Docs](https://docs.github.com/en/pages)
- [GitHub Actions для Godot](https://github.com/abarichello/godot-ci)

---

## 🎯 Швидкий старт (TL;DR)

```bash
# 1. Додай файли
git add .github/workflows/deploy-web.yml export_presets.cfg
git commit -m "Add GitHub Actions deployment"
git push

# 2. Налаштуй GitHub Pages
# Settings → Pages → Source: gh-pages branch

# 3. Чекай білд (~5 хвилин)

# 4. Відкривай https://твій-username.github.io/DialogueSYS/
```

**Готово!** 🎉
